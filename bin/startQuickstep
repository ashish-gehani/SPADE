#!/bin/bash

spade_home_path="$(cd "$( dirname "${BASH_SOURCE[0]}" )"/../ && pwd)"
script_path="${spade_home_path}/bin/manage-quickstep.sh"

echo ""
echo "Functionality moved to '$script_path'"
echo "Execute '${script_path} --help' for help"
echo ""
