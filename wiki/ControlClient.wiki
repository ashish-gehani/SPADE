#sidebar Documentation

=_Controlling the SPADE server_=

To start the client used to configure the *SPADE* server, execute the following command:

{{{
SPADE/bin/control
}}}

The following will appear:

{{{
SPADE 2.0 Control Client

Available commands:
       add reporter|storage <class name> <initialization arguments>
       add filter <class name> <index>
       add sketch <class name> <storage class name>
       remove reporter|storage|sketch <class name>
       remove filter <index>
       list reporters|storages|filters|sketches|all
       config load|save <filename>
       exit
       shutdown

-> 
}}}

----

At the outset, no reporters, storage, filters, or sketches are present. This can be seen with the `list` command:

{{{
-> list reporters
No reporters added

-> list storages
No storage added

-> list filters
No filters added

-> list sketches
No sketches added
}}}

----

To exit the client, use the `exit` command:

{{{
-> exit
}}}

----

To stop the *SPADE* server, use the `shutdown` command:

{{{
-> shutdown
}}}