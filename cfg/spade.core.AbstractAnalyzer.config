use_scaffold = true
use_transformer = true
# The differential privacy knob to control the acceptable loss in privacy
# Set to '-1' to disable i.e. do not privatize aggregate queries
# Default value '0.47712' = log(3)
epsilon=0.47712
