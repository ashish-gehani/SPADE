#allowed aggregation functions: unique, sum, minmax, list, count
#FORMAT: existing annotation name, new annotation name, aggregation function
time, time range, minmax
size, total size, sum
event id, event ids, list
storageID, storageIDs, list
unit, unit count, count
version, version count, count
