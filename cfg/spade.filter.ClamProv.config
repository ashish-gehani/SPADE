# Time window (milliseconds) to match audit and clam-prov records in
window=1000
# Time to sleep (milliseconds) while waiting for event to appear in clam-prov log
sleep=1000
# User directories (CSV) to read clam-prov logs from
userDirectories=~
