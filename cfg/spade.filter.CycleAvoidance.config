# Name of the version annotation to use. Added to all vertices.
versionAnnotationName=CAVersion
# The initial version to use for the added version annotation
initialVersion=0
# Keep all vertices even if an edge on it is going to be discarded to avoid cycles
keepAllVertices=true

