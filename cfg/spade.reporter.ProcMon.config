# Path to input log file in CSV format
#input=

# Whether to report versions of paths or not
versions=false

# Wait for log file to be completely ingested if shutdown called prematurely
waitForLog=true
