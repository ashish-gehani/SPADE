# The number of records to buffer
buffer_size=1000

# The max amount of time to buffer records. Once the time expires... the buffered data is flushed first.
buffer_ttl = 5000

# Timeout for non-blocking IO
io_timeout = 5000