# The filename where the traversal time span will be output
outputFilename=/tmp/temporal_traversal.json
# The annotation name for timestamp or event ID used for traversing the graph
annotationKey=timestampNanos
# Boolean value indicating whether to output time span (true/false)
outputTime=true
