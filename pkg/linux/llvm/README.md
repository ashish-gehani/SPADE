# Linux LLVM

This build follows the general build guidelines of a package.

This package will not work as it is out-of-date with the current LLVM infrastructure.

`src` is a symlink to code shared between different packages.
