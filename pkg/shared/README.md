# Shared

This follows the general build guidelines of a package.

The name of the platforms sharing the code should be in the shared directory's name (e.g. `linux_mac`), where possible.

Where the source files are used by a package, they should be symlinked rather than copy-pasted.
