# Mac LLVM

This build follows the general build guidelines of a subpackage.

This subpackage will not work as it is out-of-date with the current LLVM infrastructure.

`src` is a symlink to code shared between different subpackages.
