# Arg

Shared helpers for handling `String[] args` as received by a Java `main` method, for reuse across the codebase's various argument parsers (legacy and modern alike) instead of each one reimplementing the same array-handling logic.

## Testing

See [the test README](../../../../../test/java/spade/utility/arg/README.md) for what functionality is tested where.
