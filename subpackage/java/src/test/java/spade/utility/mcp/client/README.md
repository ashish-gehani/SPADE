# Tests

- `MainTest.java` — validates that the shipped default config is valid.
- `setting/ParserTest.java` — required-setting and misconfiguration scenarios, grouped by setting group (`mcp.*` vs `llm.*` vs `user.*`).
